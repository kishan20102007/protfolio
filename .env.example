# Optional. Used for sitemap.xml, robots.txt, and Open Graph absolute URLs.
# After deploying, set this to your live domain (no trailing slash).
# Example: https://your-project.vercel.app
NEXT_PUBLIC_SITE_URL=
